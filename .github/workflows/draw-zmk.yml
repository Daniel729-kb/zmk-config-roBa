# Example for using the keymap-drawer ZMK user config workflow
name: Draw ZMK keymaps
on:
  workflow_dispatch:  # can be triggered manually
  push:               # automatically run on changes to following paths
    paths:
      - "config/*.keymap"
      - "config/*.dtsi"
      - "keymap_drawer.config.yaml"
      - "keymap-drawer/**"
jobs:
  draw:
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@main
    permissions:
      contents: write  # allow workflow to commit to the repo
    # In .github/workflows/draw-zmk.yml
    with:
      keymap_patterns: "config/roBa_L.keymap"        
      parse_args: "--zmk-keyboard roBa_L"  # Changed from "roBa" to "roBa_L"
      output_folder: "keymap-drawer"                 # path to save produced SVG and keymap YAML files
      parse_args: "--zmk-keyboard roBa"              # specify the keyboard name explicitly
      draw_args: ""                                  # map of extra args to pass to `keymap draw`
